import React, { memo } from 'react'
import { FC, ReactNode } from 'react'
interface Props {
  children?: ReactNode
}
const AutomateTesting: FC<Props> = () => {
  return <div>telpmate</div>
}
export default memo(AutomateTesting)
